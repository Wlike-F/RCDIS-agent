# Backend API origin used by the browser (leave empty to use the Vite dev proxy)
VITE_API_BASE_URL=

# Dev proxy target for /api requests when VITE_API_BASE_URL is empty
VITE_PROXY_TARGET=http://localhost:8080
