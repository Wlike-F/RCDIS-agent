// Default request context sent to the backend interceptor on every call.
// A real identity layer will replace these constants later.
export const REQUEST_CONTEXT = {
  userId: 'admin',
  userName: '管理员',
  tenantId: 'lab-a'
}
